steps:
# - name: 'gcr.io/cloud-builders/docker'
#   entrypoint: 'bash'
#   args: [ '-c', 'docker build --build-arg APIKEY=$$BLZ_APIKEY --build-arg THREADS=2 -t gcr.io/$PROJECT_ID/simc:latest .' ]
#   secretEnv: ['BLZ_APIKEY']

# availableSecrets:
#   secretManager:
#   - versionName: projects/$PROJECT_ID/secrets/BlzApiKey/versions/1
#     env: 'BLZ_APIKEY'

- name: 'gcr.io/cloud-builders/docker'
  entrypoint: 'bash'
  args: ['-c', 'touch engine/simc']

artifacts:
  objects:
    location: 'gs://us.artifacts.stately-rampart-199708.appspot.com/'
    paths: ['./engine/simc']

# images: ['gcr.io/$PROJECT_ID/simc:latest']

timeout: 3600s